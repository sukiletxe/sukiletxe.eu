comments:
    allowedFields: ["author", "author_url", "content"]
    branch: "master"
    commitMessage: "Add new comment"
    filename: "{options.slug}.{@timestamp}"
    format: "yml"
    generatedFields:
        date:
            type: date
    moderation: true
    path: "data/posts"
    requiredFields: ["content"]
    # Only set this if "frontmatter" format is used
    # transforms:
        # content: "frontmatterContent"